# microtext

A single-header C library for modern text rendering through the native OS text engine (CoreText, with DirectWrite and FreeType/HarfBuzz backends planned).

Development happens on the `develop` branch.
